#include <stdio.h>
int main(int argc, char * argv[]){

	printf("how many cm u have rided\n");
	int num = 0;
	scanf("%d", &num);
	if(num > 120){
	 printf("Yellow ride\n");
	} if(num > 130){
	 printf("Green ride\n");
	} if(num > 140){
	 printf("Red ride\n");
	}








	return 0;
}
